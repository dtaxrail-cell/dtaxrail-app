name: dtr_app
description: TaxEasy - Smart, Secure, Simple Tax Filing
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  file_picker: ^11.0.2
  image_picker: ^1.2.2
  intl: ^0.19.0
  shared_preferences: ^2.3.0
  local_auth: ^2.3.0
  firebase_core: ^4.9.0
  dio: ^5.8.0+1
  firebase_auth: ^6.1.0
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/tax_filing_banner.png